import Foundation

// Minimal stub for Rork's build pipeline, which regenerates this file from
// EXPO_PUBLIC_* environment variables at build time. Do not add other members
// here — they will be wiped. Use TokenStore for runtime-settable tokens.
enum Config {
    static let EXPO_PUBLIC_APIFY_TOKEN: String = ""
}
